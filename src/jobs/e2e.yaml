description: >
  This job will run e2e test based on parameter
# What will this job do?

executor: default

parameters:
  suite:
    type: string
    default: "sanity"
    description: "parameter to run functional or sanity suite"
steps:
  - greet:
      to: << parameters.to >>
	